[package]
name = "core"
version = "0.1.0"
edition = "2024"
description = "Codec registry: type-erased transcoders on a graph with A* path finding between data formats."

[dependencies]
thiserror = "2"
petgraph = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
itertools = "0.14.0"
anyerror = "0.1.13"
